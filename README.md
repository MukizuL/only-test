## Перед запуском

Необходимо назначить в файле .env несколько переменных окружения: SMARTCAPTCHA_SERVER_KEY и SMARTCAPTCHA_CLIENT_KEY.

## Быстрый запуск через Docker Compose

```bash
docker compose up --build
```

После запуска сервис будет доступен по адресу `http://localhost:8080`.
